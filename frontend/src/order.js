import React from "react";

const Order = () => {
    return (
        <div>
            <h2>Your Order Details</h2>
            {/* Display the order details here (e.g., quote, time window, price) */}
        </div>
    );
};

export default Order;
